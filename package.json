{
  "name": "alert-proxy",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

No backticks, no extra text. Save it, then back in the terminal run:
```
git reset --hard && git pull && npm install && node server.js

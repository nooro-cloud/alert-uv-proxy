{
  "name": "alert-proxy",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

Then in the Codespace terminal:
```
npm install && node server.js
